class PlayerCard {
  int playerCardId;
  final String volleyballerName;
  final String description;
  final String photoUrl;

  PlayerCard(
      {required this.playerCardId,
      required this.volleyballerName,
      required this.description,
      required this.photoUrl});
}